# Submission

Initial CRAN submission of `cohort` (version 2026.5.10).

## Test environments

- Local: Ubuntu 24.04, R 4.6.0
- win-builder: R-devel
- R-hub: linux, windows, macos

## R CMD check results

Local `R CMD check --as-cran`: 0 errors | 0 warnings | 1 note.

The single note is "New submission", which is expected for an
initial submission.

## Downstream dependencies

None (this is a new package).
